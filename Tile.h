#pragma once
#include"Texture.h"
#include<fstream>
/*enum TileTypes
{
	TILE_NOTHING = 0,
	TILE_GROUND_UP_LEFT,
	TILE_GROUND_UP_MID,
	TILE_GROUND_UP_RIGHT,
	TILE_GROUND_DOWN_LEFT,
	TILE_GROUND_DOWN_MID,
	TILE_GROUND_DOWN_RIGHT,
};
const int TILE_WIDTH = 80;
const int TILE_HEIGHT = 80;
const int TOTAL_TILES = 360;
const int TOTAL_TILE_SPRITES = 7;
class Tile
{
private:
	SDL_Rect Collider;
	int type;
	SDL_Rect gTileClips[TOTAL_TILE_SPRITES];
public:
	Tile(int x, int y, int tileType);
	void render(SDL_Rect& camera, SDL_Renderer* Renderer, Texture& tileTexture);
	SDL_Rect getCollider();
	int getType();
	bool setTiles(Tile* tiles[]);
};*/